[build]
  base = "frontend"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"


# Frontend -> Backend proxy:

[[redirects]]
  from = "/api/*"
  to   = "https://bankingexpertbe.netlify.app/.netlify/functions/api/:splat"
  status = 200
  force  = true
